# Nav2 MCP Server Configuration
# Copy this file to .env and adjust values as needed

# Transport mode for the MCP server
# Options: 'stdio' (default for local integration) | 'http' (for containerized/network deployment)
TRANSPORT_MODE=stdio

# HTTP transport configuration (used when TRANSPORT_MODE=http)
# Host address for HTTP server binding
HTTP_HOST=0.0.0.0

# HTTP port for the server
HTTP_PORT=3001

# Logging level
# Options: DEBUG | INFO (default) | WARNING | ERROR | CRITICAL
LOG_LEVEL=INFO
